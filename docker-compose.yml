services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile.fresh
    image: smart-library-backend:latest
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
      - PORT=3000
    restart: unless-stopped
    command: npm run dev
